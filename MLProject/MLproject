name: Heart_Disease_Classification_Project
python_env: requirements.txt

entry_points:
  main:
    parameters:
      model_name:
        type: string
        default: "all"
    command: python modelling.py --model_name {{model_name}}
